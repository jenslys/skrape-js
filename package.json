{
  "name": "skrape-js",
  "version": "1.0.0",
  "description": "A TypeScript library for easily interacting with Skrape.ai",
  "repository": {
    "url": "https://github.com/jenslys/skrape-js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "prepare": "npm run build",
    "dev:test": "ts-node -r dotenv/config src/test.ts",
    "dev": "ts-node -r dotenv/config src/dev.ts"
  },
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "zod": "^3.0.0",
    "zod-to-json-schema": "^3.22.4",
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.9.2",
    "dotenv": "^16.3.1"
  }
}
